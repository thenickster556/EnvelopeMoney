# Mountain Money

Mountain Money is an Android pond-budgeting application (user-facing “ponds”; internal types may still read `Envelope`) focused on monthly budget tracking, transaction history, pond-to-pond transfers, recurring transactions, optional per-pond **Account** balances for bank reconciliation, and **bills days** (recurring days-of-month plus a filter through the last bills day).

## Repository Memory System
This repository uses a lightweight project memory system so AI-assisted development has stable architectural context, user-flow context, and task history before code changes are made.

## Documentation Index
- `docs/PROJECT_INDEX.md`
- `docs/ARCHITECTURE.md`
- `docs/DATA_SCHEMA.md`
- `docs/features.md`
- `docs/user-flows.md`
- `docs/AI_CHANGE_PROTOCOL.md`
- `state/TASK_STATE.json`
- `prompts/codex_rules.md`

## Development Expectations
- Read the repository memory files before changing code (`docs/AI_CHANGE_PROTOCOL.md`, `prompts/codex_rules.md`).
- Verify changes against architecture, feature behavior, and user flows.
- Prefer **theme attributes** (`?attr/colorControlNormal`, `@color/mountain_primary`, Material overlays) for new buttons and spinners so UI stays consistent with DayNight.
- Add or update tests when behavior changes.
- Confirm compile/test status before finalizing.
- Update docs and task state whenever behavior or structure changes.
- Stage commits locally with a structured message; do not push automatically.

## Visuals
### Repository Memory Structure
```text
EnvelopeMoney/
+-- docs/
+-- state/
+-- prompts/
+-- README.md
+-- app/
+-- web/          Node + Mongo + HTML demo

```

### Month Rollover Flow
```text
Stored state -> sanitize -> decide target month -> rebuild target month on deep copy -> adopt repaired state
```

### Test Surface
```text
MonthRolloverHelperTest
MonthTrackerTest
BillsDayAnchorTest
MoneyMathTest
PondBankReconciliationHelperTest
CommentHistoryTest
OcrAmountWeightsTest
OcrAmountLearnerTest
ReceiptFieldParserTest
SpendAnalysisHelperTest
HistoryTransferTotalsTest
```

## Change Log
- 2026-09-17: **Inbound transfers stay out of Total:** Money transferred into a destination pond is omitted from history **Total** and shown on the transfers panel as **To dest: -$X.XX** so inbound cash is visible. The To line is not zeroed when that destination is selected. `HistoryTransferTotals` + web port. Java helper 3/3; `node --test` 143 pass. Gradle 6.7.1 blocked on JDK 25.
- 2026-09-17: **Assigned unused photos leave the unused list; See all unused keeps amount/date:** **Use this picture** on a previously unused JPEG reserves URI and filename so it does not reappear in later unused / See all unused lists (including when MediaStore later lists the same name as `content://`). See all unused keeps the transaction amount and date in the chooser message for OCR cross-check. `ReceiptAlbumMatcher` + `ReceiptCandidateSummary` tests (43 via javac/JUnit; Gradle 6.7.1 blocked on JDK 25). Android only. Web unchanged.
- 2026-09-17: **Bills-period filter shows transfers:** Selecting the history bills-period filter turns transfer visibility on so pond-to-pond moves appear in that window. Clearing the filter leaves the transfer toggle unchanged. Restored active filter on launch also shows transfers. `BillsPeriodFilterUi` + tests (2/2 via javac/JUnit; Gradle 6.7.1 blocked on JDK 25). Android only. Web unchanged.
- 2026-09-16: **Web demo local-folder storage:** Additive client LocalFileStorage + ReceiptStorage beside Mongo/GridFS. Default remains server receipts. Opt-in **Receipts on this device** uses capability detection (live folder, directory import, file pick, working copy). Express binds `HOST` 0.0.0.0 with Local/Network URLs and optional HTTPS. SQLite learning and GridFS replace-by-id unchanged. Android unchanged. Tests first: `npm test` 139 pass.
- 2026-09-16: **See all unused pictures in the list; faster thumbs and amounts:** **See all unused pictures (N)** sits after the last thumb in the chooser (not a Neutral button next to Cancel). Leftovers always open the drawer, even for one ±1 file. Dedicated 2-thread thumb pool + LRU; in-place re-sort; ranking OCR on `decodeSampled` 1280; `file://` two-pass subsample; preview first paint ~1600. Unique auto-bind, **Use this picture**, no Files picker, no new Gson. Tests first. Full suite 309 tests / 2 baseline failures; recovery coverage 97.59% lines / 80.75% branches; lint baseline 43 unchanged. assembleDebug + git diff --check pass. Device UI not re-run.
- 2026-09-16: **Append close OCR unused, then all leftover unused:** The chooser main list stays unused ±1. Background OCR (same pipeline + learned weights) appends leftover unused pictures whose printed total is exact, within $2 or 10%, or within $5 or 25% (`$20` vs `$21.66` close; `$29.28` vs `$21.66` not). **None of these** lists every remaining unused picture with the OCR'd `$`. **Use this picture** nudges sidecar OCR weights. Unique auto-bind, no Files picker, no new Gson. Tests first. Full suite 307 tests / 2 baseline failures; recovery coverage 97.59% lines / 80.75% branches; lint baseline 43 unchanged. assembleDebug + git diff --check pass. Device UI not re-run.
- 2026-09-16: **Tap unused-album fallback (Open receipt picture loop):** Device still showed **Open receipt picture** because `unusedNearby` required capture day ±1 and emptied out on restricted photo access. Unique auto-bind stays ±1-only. Tap/Retry now lists ±1 first, then **every unused Pictures/Mountain Money file** (minus reserved); restricted-but-listable albums still offer what they can see; `inspect()` uses `ReceiptBitmapLoader` so `file://` disk rows survive the chooser. Tests first. Full suite 305 tests / 2 baseline failures; recovery coverage 97.50% lines / 81.02% branches; lint baseline 43 unchanged. assembleDebug + git diff --check pass. Device UI not re-run — rebuild the APK before judging the tap.
- 2026-09-16: **Unused nearby picker on tap, not the Retry loop:** Tapping a dead receipt no longer loops **Retry automatic search** while unused Pictures/Mountain Money shots from that day ±1 exist. `unusedNearby` feeds the existing OCR-ranked chooser; unique auto-bind and **Use this picture** are unchanged. Copied files with a wrong DATE_TAKEN now probe EXIF anyway (epoch → EXIF → DATE_TAKEN → DATE_ADDED). Tests first. Full suite 303 tests / 2 baseline failures; recovery coverage 97.60% lines / 80.79% branches; lint baseline 43 unchanged. assembleDebug + git diff --check pass. Device UI not re-run.
- 2026-09-15: **OCR-ranked candidates, EXIF capture dates, gallery move restored (recurring regression):** Broken receipts whose pictures were copied into the folder weeks after the spend showed the MISSING retry dialog because capture time fell back to `DATE_ADDED` (the copy day), emptying the day-±1 window. Capture precedence is now filename epoch → `DATE_TAKEN` → **EXIF `DateTimeOriginal`** (header-only probes for undated rows, `exif=K` logged) → `DATE_ADDED`, plus a `receipt chooser: status=…` tap-path log. While the picker is open, background OCR scores every candidate via the existing pipeline and the pure `ReceiptCandidateScorer` (exact amount 100 / close 40 / merchant tokens ≤50 / date 20), re-sorting rows live with badges (`Amount matches · $12.50` / `Close amount · $12.75` / `No amount read`); generation guards drop stale reorders; attach still only **Use this picture**. **Recurring regression (third occurrence) fixed:** the `GetContent` swap silently disabled both delete paths so gallery imports duplicated; the dead `takePersistableUriPermission` is removed, `isMediaStoreImagesUri` recognizes the media documents provider (its long-failing baseline test is now green — **baseline failures 3 → 2**), and on Android 11+ a survived MediaStore source triggers `MediaStore.createDeleteRequest` — one system consent sheet finishes the move; Cancel keeps both and is asked once per photo. Tests first (scorer/EXIF/consent/deleter). Full suite 298 tests / 2 baseline failures; recovery coverage 97.49% lines / 80.38% branches; lint baseline 43 unchanged. assembleDebug + git diff --check pass. Chooser OCR/consent UI on-device checklist pending.
- 2026-09-15: **Red list icon when a receipt is not uniquely loaded:** After existing startup `resolveAll`, history photo icons tint `colorError` when the JPEG is not uniquely loaded (MISSING / AMBIGUOUS / CORRUPT / PERMISSION_REQUIRED). Unique binds stay the normal tint. Rows without a stored URI stay icon-less. No new Gson keys, no Files picker, no launch choosers. Tap flow unchanged. History always refreshes after repair even if `apply()` is 0. Tests first (`ReceiptRowUi.receiptNeedsAttention`). Full suite 287 tests / 3 baseline failures; recovery coverage 97.54% lines / 82.04% branches; lint baseline 43 unchanged. assembleDebug + git diff --check pass. Device UI not re-run.
- 2026-09-15: **Fullscreen pick cue + ranked nearby receipt options:** Candidate `[FS]` keeps a translucent comment/pond/amount reminder on the JPEG (empty comment = **No comment**); the opaque header + Select screen is unchanged. Unique auto-match is unchanged; when it cannot pick one unused file, unused Pictures/Mountain Money shots from that day ±1 (not already bound) appear in the in-app chooser, closest capture time first. Tests first. Full suite 285 tests / 3 baseline failures; recovery coverage 97.54% lines / 82.04% branches; lint baseline 43 unchanged. assembleDebug + git diff --check pass. Device UI not re-run.
- 2026-09-15: **Gallery photo picker, off-thread receipt IO, web transfer save, GridFS rotate, candidate fullscreen:** Add/edit **From gallery** uses `GetContent` so Photos/gallery opens, not Files; import still persists the Mountain Money album URI (preview/recovery never a picker). Picker byte read and camera decode/save moved off the UI thread. Web `saveTx` validates transfers before insert and relocates edited transfer sources when the pond changes. GridFS PUT uploads a temp file then replaces the original id. Candidate slides get a chrome-hiding fullscreen button (exit overlay / Back restores Select). Tests first (`transactionSave.test.js`, `gridFsReplace.test.js`, `ReceiptPreviewImmersiveTest`). Full suite 274 tests / 3 baseline failures; recovery coverage 97.40% lines / 83.33% branches; lint baseline 43 unchanged. `npm test` 108 pass. assembleDebug + git diff --check pass. Gallery/camera/fullscreen UI not re-run on a device.
- 2026-09-14: **Preview toolbar tap targets:** Close / swap / save / rotate icons grew from packed 48dp to **56dp** with **8dp** gutters and `fitCenter` glyphs so adjacent actions are easier to hit. Close sits with the title in a weighted cluster so attached-mode actions stay on the right. Tests first (`ReceiptPreviewToolbarMetricsTest`). Full suite 270 tests / 3 baseline failures.
- 2026-09-14: **Candidate swipe + top hint + themed Select:** Restoration candidate check browses with a fit-scale horizontal swipe (same `showCandidate` as the arrows; no wrap, no attach). The pinch/pan hint moved into the top chrome so it cannot cover **Use this picture**. That button is `mountain_primary` with a white label on the always-black preview (night `colorOnPrimary` is not used). Tests first (`ReceiptCandidateSwipeTest`, 10 cases). Full suite 267 tests / 3 baseline failures; recovery coverage 97.40% lines / 83.33% branches; lint baseline 43 unchanged.
- 2026-09-14: **Stable swap pool + icon toolbar:** "Choose different" said "No other pictures match" after any pick — the stored filename now names the picked file, so the single matcher pass resolved the identity tier to the attached file and never reached the date pool, and the session set never released replaced picks. `swapCandidates` rebuilds the pool as an identity scan (exact ∪ normalized ∪ epoch token, no tier short-circuit) union a nameless-claim date pass, minus the attached file and other rows' reserved references; replaced picks are released so they reappear. The preview toolbar's four text buttons (jumbled on small screens) became 48dp white vector icons (swap/save/rotate-left/rotate-right) with the labels kept as contentDescriptions and 40%-alpha disabled dimming; "Use this picture" stays the single text action. Tests first (3 rewritten + 4 new red cases). Full suite 257 tests / 3 baseline failures; recovery coverage 97.40% lines / 83.33% branches; lint baseline 43 unchanged.
- 2026-09-14: **Fullscreen candidate check + later swap:** Picker thumbnails 56→80dp with the transaction summary in the message, and a row tap (or a lone readable survivor) now opens a fullscreen check instead of attaching — transaction lines in the header (`Lunch · $12.50` / `Food · Sep 7, 2026` via new `ReceiptCandidateSummary`), pinch-zoom below, `‹`/`›` arrows with `n of m` counter (disabled at ends), `Use this picture` enabled only when decoded, close/back minimizes to the still-open picker. The attached-receipt preview gains a `Choose different` button that re-runs the matcher for that one receipt (`ReceiptReferenceRepair.swapCandidates`, attached file excluded, denied/empty → empty) and reuses the picker → check flow; empty pool toasts. One `ActivityResultLauncher` serves all preview launches; candidate ClipData grants cover arrow navigation; stale results are ignored. Tests first (6 added; two red-state bugs caught: `$-3.20` formatting, `getStringExtra` vs `String[]`). Full suite 254 tests / 3 baseline failures; recovery coverage 97.13% lines / 83.83% branches; lint baseline 43 unchanged.
- 2026-09-14: **Ambiguity picker + sub-second recovery:** AMBIGUOUS matches no longer dead-end in a retry dialog — the matcher now carries the competing files (`Result.alternatives`), the tap flow re-verifies them, drops unreadable candidates (a lone survivor auto-opens), and shows a picker (thumbnail, filename, capture time) where a tap attaches the verified pick, toasts, and previews; Cancel changes nothing; session picks can't be double-assigned. Search speed: `inspect` verifies with one stream open + header decode instead of a second full sampled decode, and the tap path resolves only that row's entries instead of every receipt in the app (whole-app sweep stays at startup). Pass duration + album inventory log under `EnvelopeMoney`. Full suite 248 tests / 3 baseline failures; recovery coverage 97.19% lines / 84.63% branches; lint baseline 43 unchanged.
- 2026-09-14: **Renamed/copied receipts recover:** The same-day fallback could never fire for renamed photos because repair vetoed any date match whose album filename differed from the stored name. Matching now binds unique identity without date evidence (exact name → normalized name folding case/`.jpeg`/percent-encoding/` (1)` copy suffixes → shared epoch digit-run), the veto became a listing-vs-inspection race guard that also updates the stale stored filename, and a lone claim retries one day either side. `Pictures/Mountain Money` matching is path-casing-insensitive, an empty granted index triggers one best-effort rescan, Android 14 "Select photos" access is restricted-but-listable, and each repair pass logs its album inventory (`EnvelopeMoney` tag). Full suite 240 tests / 3 baseline failures; recovery coverage 97.00% lines / 84.11% branches; lint baseline 43 unchanged.
- 2026-09-12: **Unique same-day album match on transfer-patch:** Port stored-URI open (OpenDocument persistable read grant, content-only Intent data, one-shot byte decode, no re-import). Dead pointers can repair from `Pictures/Mountain Money` when a filename is unique or exactly one unused file shares the transaction’s local calendar day. Two same-day leftovers stay ambiguous. Tests: `ReceiptAlbumMatcherTest`, `ReceiptReferenceRepairTest.resolveAll*`. Receipt unit tests 135/1 baseline `ReceiptSourceDeleter` failure. Recovery coverage 97.61% lines / 84.86% branches.
- 2026-09-12: **Preview URI grants from earlier open path:** Stored-URI extra-only launch was close but preview still could not read picker/document URIs. Restore `OpenDocument` + `takePersistableUriPermission` (removed in `7d4c0ad`) and `content://` `setData` + read grant (from `0afe24a` launch, without `file://` persist). Decode reads the URI **once** into bytes (`0afe24a` `ReceiptUriStreams` approach) because some providers allow only one open. Preview tap does not re-import. Tests: `ReceiptPreviewActivityTest.intentDataUri_onlyContentScheme`, `ReceiptBitmapLoaderTest.decodeSampled_readsUriThatAllowsOnlyOneOpen`.
- 2026-09-10: **Photo access for legacy receipt recovery:** Opening an app-owned picture no longer counts as library access. Launch/resume requests `READ_MEDIA_IMAGES` / `READ_EXTERNAL_STORAGE` when stored receipts exist. A partial owned-only MediaStore list is treated as permission-required for other filenames so yesterday/older `Pictures/Mountain Money` files can be found. Album `RELATIVE_PATH` matches with or without a trailing slash. Tests: `ReceiptReferenceResolverTest`, `AndroidReceiptSourceTest`. Full suite 192/195 passed (3 baseline failures). Recovery coverage 179/185 lines (96.76%), 156/186 branches (83.87%).
- 2026-09-07: **Receipt import keeps album URI:** Gallery import no longer rolls back to a dead picker grant when the source cannot be deleted. Persist the MediaStore `content://` insert URI (optional `#MountainMoney_*.jpg` fragment). Preview opens that URI only — ContentResolver, leftover `file://`, then `DISPLAY_NAME` lookup. Tests: `ReceiptPickerUriNormalizerTest.persistUriAfterImport_keepsAlbumCopyWhenPickerSourceRemains`.
- 2026-09-05: **Restore upstream receipt preview:** Persist the MediaStore `content://` insert URI on API 29+ (not `file://` folder paths). Preview probes that URI and launches `ReceiptPreviewActivity` with `EXTRA_IMAGE_URI` only. `FileInputStream` is used only for leftover `file://` album paths. Removed `ReceiptFolderOpener` / `ReceiptUriStreams`. Tests: `ReceiptBitmapLoaderTest.usesFilePathFallback_onlyFileScheme`.
- 2026-08-20: **Spend analysis charts:** Top-bar chart icon opens Analysis (Last 3/6/12, pond chips, include-transfers) with spend-by-month bars, over-Limit pond-months, by-pond bars, and this-month snapshot. Over budget uses `Envelope.limit` only. Default spend excludes transfer rows. No new persistence. Tests: `SpendAnalysisHelperTest`, `web/test/spendAnalysis.test.js`. Local full Gradle still needs JDK **8–15**; helper unit tests verified via JUnit on JDK 21.
- 2026-08-20: **Comment typeahead + OCR amount learning:** Add/edit dialogs remember comments in sidecar SQLite (`mountain_money_learning.db`) with an inline 3-row suggestion list; OCR amount corrections nudge a float32 weight vector used by `ReceiptFieldParser` fallback scoring. Web demo uses the same file schema per user (`web/data/learning/<userId>.db`). Envelope Gson/Mongo unchanged. Tests: `CommentHistoryTest`, `OcrAmountWeightsTest`, `OcrAmountLearnerTest`, parser goldens, `web/test` comment/learning suites.
- 2026-08-19: **Web demo seed + empty list:** Five ponds and 13 months of spending, transfers, and split purchases for empty accounts and `alice-demo`; transaction list shows **No transactions to display** when a month/filter has no rows.
- 2026-08-18: **Web demo (localhost + MongoDB):** `web/` Node/Express + Mongo per registered account; mobile HTML/CSS matching ponds, payday Remaining, bills filter, transfers/splits, receipts + Tesseract/`ReceiptFieldParser`. Domain JS tests ported from JUnit. Android unchanged. See `docs/WEB_DEMO.md`.
- 2026-07-21: **Gallery receipt preview reopen:** Context-aware Mountain Money ownership (`DISPLAY_NAME` / `RELATIVE_PATH` / path markers); Preview and list photo open the stored URI without re-import/move (avoids deleting the MediaStore row). Tests: `ReceiptPickerUriNormalizerTest`. Docs updated.
- 2026-07-14: **Payday Remaining / Still-to-deposit formula:** With paydays + Account, Remaining = Account checkpoint + unlocked payday Limit slices − month spend; Still to deposit = unpassed payday slices; paydays count on/after their day and reset each month. `PondBankReconciliationHelper` + `MainActivity` wiring + `PondBankReconciliationHelperTest`. Docs updated. Local full Gradle still needs JDK **8–15**; helper unit tests verified via JUnit on JDK 21.
- 2026-07-14: **Intent (in progress):** Fix payday Remaining / Still-to-deposit — Remaining = Account checkpoint + unlocked payday slices − month spend; Still to deposit = unpassed payday slices of Limit; count paydays only on/after their day; reset each month. Helper + MainActivity + tests + docs.
- 2026-03-21: Initialized repository memory system.
- 2026-03-21: Added `MonthRolloverHelper`, startup rollover sanitization, envelope state repair helpers, and month normalization tests.
- 2026-03-21: Verification is currently blocked locally because this machine only exposes Java 25 while Gradle 6.7.1 requires an older compatible JDK.
- 2026-04-12: Mountain Money rebrand (strings/theme), custom outlined top bar with bills-days calendar and recalculate, per-pond `accountBalance`, bills-period filter next to transfers, `BillsDayAnchor` + `BillsDayAnchorTest`, `docs/PROJECT_INDEX.md`, and expanded docs. Local `./gradlew` may still require a JDK compatible with Gradle 6.7.1.
- 2026-04-12: Repository memory protocol refreshed (`PROJECT_INDEX`, `AI_CHANGE_PROTOCOL`, `codex_rules` with precheck + finalization). UI aligned to theme: `MaterialAlertDialogBuilder`, spinner overlay, `colorControlNormal` tints, totals row + recurring chip colors, `DATA_SCHEMA` note on persistence vs SQL.
- 2026-04-12: Bills-days calendar weekday header text now uses `mountain_primary` instead of platform holo green.
- 2026-04-12: Full control theme pass: `btnAddTransaction` tint; spinner overlay DayNight (`values-night/colors`, non-Light overlay parent); recurring calendar unselected cell + totals row night colors; `MainActivity` `resolveThemeColor` for chips, monthly grid, bills-day cells, transfer/bills icon filters (`colorControlNormal` when inactive).
- 2026-04-12: Custom top bar DayNight: `values-night` `mountain_top_bar_fill` / `mountain_top_bar_stroke`; `tvAppTitle` uses `?attr/colorPrimary` (`app_bar_main.xml`).
- 2026-04-12: Carry-over invariant: `Envelope.limit` stays the user monthly budget; unused funds roll into **remaining** / month snapshots only. `reset(true)` no longer inflates limit; `initializeMonth` missing-month seed aligns with rollover; `MonthRolloverHelper` / `MainActivity` recalculate (Option A) documented; tests (`MonthRolloverHelperTest`, `EnvelopeCarryoverInvariantTest`).
- 2026-04-13: Bills-period filter behavior: **start** date snaps to `BillsDayAnchor`, **end** date is **today** (was incorrectly setting end to anchor). `applyPersistedBillsFilterState` matches; prefs still store pre-filter range for toggle-off restore. Docs, `strings.xml`, `PrefManager` javadoc updated.
- 2026-04-13: Add/edit transaction dialogs: recurring section uses themed chips (`recurring_*_ripple`, `drawable-v21` ripples), `AppCompatCheckBox` / `AppCompatTextView` + tinted calendar icon; `applyIconMaterialDialogActions` (check + close drawables) on new/edit and recurring sub-dialogs.
- 2026-04-14: Transfer **Transfer To** list uses `TransferDestinationList` (source pond excluded; trim-safe). **BillsDayAnchor**: when only one bills day is set and today is that day, anchor is that day in the previous month (clamped); tests `TransferDestinationListTest`, `BillsDayAnchorTest`.
- 2026-04-14: Transactions header transfer totals **spinner** lists destination ponds only (amount transferred **to** each), not “from” source rows; `MainActivity` + docs.
- 2026-04-19: **Manifest:** `MainActivity` sets `android:exported="true"` (required for API 31+ when a launcher `intent-filter` is present; fixes `processDebugMainManifest` merger failure).
- 2026-04-20: **Receipt preview fullscreen:** `ReceiptPreviewActivity` + `ReceiptZoomImageView` (pinch-zoom, pan, double-tap refit) and rotate buttons; `ReceiptBitmapLoader` shared sampling; removed dialog preview layout. Docs updated.
- 2026-04-21: **Receipt OCR heuristics:** `ReceiptFieldParser` picks a **better store line** (skip phone, URL, guest/table, street, zip-only), **title-cases** typical ALL CAPS names, and for **restaurant / receipt** capture prefers **amount due** / the **last** “total” line so the autofill amount tracks the **final** charge after tip when labels support it. `ReceiptFieldParserTest` goldens updated. Verified locally: Gradle 6.7.1 requires a compatible JDK; Java 25 fails settings compilation (“Unsupported class file major version 69”).
- 2026-04-29: **Receipt preview:** **Save rotation** — **view-only** 90° steps until save; **Material** replace + discard-on-close; **ReceiptRotatedJpegWriter.writeRotatedJpegOverwrite** decodes from the same URI, **Matrix**-rotates, writes JPEG **92** via `openOutputStream(uri, "w")`, reloads preview. Failures: **Material** alert. **ReceiptFieldParser** verified; **ReceiptFieldParserTest** extended (`merchant_skipsHttpUrlLine`, `receipt_prefersBottomStrongPayThisAmount`). **`./gradlew`:** Java **25** still fails Gradle 6.7.1 settings (“Unsupported class file major version **69**”); use JDK 8–15.
- 2026-04-19: **Receipt preview UX:** Add/edit dialogs — **Preview receipt**, **Remove receipt**, shared `receiptDialogHostView` for OCR; edit persists `receiptImageUri`; list-row photo icon + preview; `ReceiptRowUi` + `ReceiptRowUiTest`. **Recurring chips:** `recurring_chip_fill_unselected` DayNight + unselected label uses `textColorPrimary` (fixes dark-mode white-on-white). Docs updated. `./gradlew` still requires JDK 8–15 here.
- 2026-04-19: **Build:** `com.google.android.material:material` pinned to **1.6.1** (was 1.8.0). Material 1.8+ embeds `<macro>` tags that **AGP 4.2** cannot merge (`mergeDebugResources` / “Can't determine type for tag … m3_comp_assist_chip_container_shape”).
- 2026-04-15: **Receipt capture** in new-transaction dialog: CameraX (`ReceiptCaptureActivity`), gallery pick, `MediaStore` **Pictures/Mountain Money**, on-device OCR via `OcrEngine` (ML Kit Latin default; Paddle-shaped `PaddleOcrAdapter`), `ReceiptFieldParser` + tests, optional `Transaction.receiptImageUri`. **minSdk 21**, compile/target **33**. See `docs/receipt-ocr.md`. Local `./gradlew` still needs JDK 8–15 for Gradle 6.7.1.
- 2026-04-30: **Add/edit transaction dialogs:** **Receipt icon toolbar** (camera / gallery / preview / delete) at the top with optional horizontal scroll; form body in **`BoundedNestedScrollView`** capped by **`dialog_transaction_scroll_max_height`** so recurring + **Transfer To** stay reachable with IME / short windows; **`wireReceiptRow`** / **`syncReceiptActionUi`** use **`View`** for those controls. `./gradlew` still fails on this machine with Java **25** (Gradle 6.7.1 / unsupported class file major version **69**); use JDK **8–15** to compile.
- 2026-05-11: **Grouped transfer buckets:** add/edit transaction dialogs now support multiple transfer buckets from one source total with slider, +/- step buttons, and manual amount entry; persistence now uses `Transaction.transferBucketId` plus bucketed `Envelope.TransferData`; transfer history keeps one source summary row and one mirror row per destination bucket; tests added for grouped transfer draft/sync helpers. Local Gradle verification is still blocked here because the available JDKs are Java **21** and Java **25**, while Gradle **6.7.1** requires Java **8-15**.
- 2026-05-11: **Transfer dialog UX polish:** in-dialog transfer pond selectors now use anchored exposed dropdowns, grouped bucket controls moved to amount + slider rows with scale landmarks, validation waits for interaction/save, transfer mode auto-scrolls into view, and the modal window uses stronger dim + resize behavior. Local Gradle verification remains blocked here because the available JDKs are Java **21** and Java **25**, while Gradle **6.7.1** requires Java **8-15**.
- 2026-05-11: **Transfer dialog compact-width follow-up:** transfer bucket sliders now stack below the amount row again, compact screens collapse the landmark row to fewer labels, and duplicate dropdown labels were removed to reclaim vertical space. Local Gradle verification remains blocked here because the available JDKs are Java **21** and Java **25**, while Gradle **6.7.1** requires Java **8-15**.
- 2026-05-17: **Split purchase + tabbed transaction dialogs:** add/edit use **Spending / Transfer / Split purchase** tabs (time **One-time / Recurring** only on Spending). Split purchases persist as linked positive slices (`splitPurchaseGroupId`, `splitPurchaseBucketId`) via **`SplitPurchaseSyncHelper`** / **`SplitPurchaseGroupDraft`**; history rows can **expand** an inline breakdown per group. **Delete** on any slice removes the whole split group. Local `./gradlew` still fails on Java **25** (Gradle **6.7.1** / unsupported class file major version **69**); use JDK **8–15** to compile.
- 2026-05-17: **Recurring UX:** Removed the **This is recurring** checkbox — **One-time / Recurring** is chosen only via the Spending **time** tabs. **`dialog_transaction.xml`** is aligned with **`dialog_new_transaction.xml`**.
- 2026-05-17: **Bills-period filter:** With one configured bills day, **`BillsDayAnchor`** always uses that day in the **previous** month (e.g. bills on the 12th, today May 13 → start **April 12**, not May 12). Multiple bills days unchanged (latest on or before today). **`BillsDayAnchorTest`** updated.
- 2026-05-17: **Transaction dialog tab order:** **Spending / Transfer / Split** (and **One-time / Recurring** under them on Spending) moved **below the comment** field so pond, date, amount, and note are filled before choosing mode.
- 2026-05-17: **Recurring chrome:** Frequency and day controls default to hidden and only appear on **Spending** + **Recurring**; explicit visibility sync after tab setup fixes **TabLayout** not firing **onTabSelected** when the default tab is already selected.
- 2026-05-19: **Payday bank reconciliation:** Top-bar calendar opens **Bills days / Paydays** tabs (`paydays_json`). With paydays + pond **Account**, `PondBankReconciliationHelper` + `MoneyMath` (2 dp) auto-sync **Remaining** to still-to-deposit; footer and edit pond show one line (**In bank | Still to deposit | Target**). Tests: `MoneyMathTest`, `PondBankReconciliationHelperTest`. Local `./gradlew` still requires JDK **8–15** (Java **25** fails Gradle **6.7.1**).
- 2026-05-19: **Pond reconciliation UI:** Removed duplicate **Target** from footer, pond row, and edit preview; reconcile line shows **In bank | Still to deposit** only (limit already on pond row).
- 2026-05-19: **Receipt OCR brand fallback:** Removed **Unknown merchant**; when scoring finds no brand, comment uses the **first significant word** on the **topmost** OCR line. **`ReceiptFieldParserTest`** updated.
- 2026-05-19: **Receipt OCR brand comment:** OCR lines sorted top-to-bottom with **line height**; comment prefills **store brand only** (top/prominent line, junk filters, Welcome/Store# cleanup); fills comment **only when empty**. **`ReceiptFieldParserTest`** extended.
- 2026-05-19: **Receipt OCR tips:** **`ReceiptFieldParser`** composes **subtotal + tax + tip** and **pre-tip total + tip** when tip lines follow a pre-tip **Total**, prefers **total paid** / **amount due**, and ignores suggested-tip guide lines. **`ReceiptFieldParserTest`** extended. See **`docs/receipt-ocr.md`**.
- 2026-05-19: **Transaction dialog drafts:** Switching **Spending / Transfer / Split purchase** tabs no longer clears split slices; transfer and split in-dialog work is preserved until save. **Transfer** defaults to 100% on the first bucket; **Add transfer bucket** redistributes with first-ceiling integer percents via **`MoneyMath.splitIntegerPercentsFirstCeiling`** / **`splitTotalByPercents`**. **Split purchase** prefills purchase total from amount when empty and applies the same even-split defaults. Tests extended in **`MoneyMathTest`**. Local `./gradlew` still requires JDK **8–15** (Java **25** fails Gradle **6.7.1**).
- 2026-05-19: **No pond found fix:** **`PondLookup`** resolves pond names with trimmed equality; add/edit transaction save uses **`requireSelectedPond`** so **No pond found** only appears when the selected name is absent from the live pond list (e.g. deleted pond). **`PondLookupTest`** added. Local `./gradlew` still requires JDK **8–15**.
- 2026-07-03: **Gallery receipt import (2nd fix):** Re-show transaction dialog after gallery/camera returns (dialog was dismissed under picker); read picker bytes on main thread then import off-thread; `normalizeImportFromBytes`; toast when receipt is ready to save.
- 2026-07-03: **Gallery receipt import follow-up:** Persist dialog host across gallery picker return; keep OK disabled until OCR finishes; stream-copy JPEG imports; stronger source delete via `DocumentsContract`; clearer empty-amount validation.
- 2026-07-03: **Gallery receipt import fix:** Main-thread import before OCR; OK disabled until receipt URI attached; **move** into Pictures/Mountain Money when source delete succeeds (`ReceiptSourceDeleter`). Tests: `ReceiptSourceDeleterTest`, `ReceiptPickerUriNormalizerTest` updated. Local `./gradlew` still requires JDK **8–15**.
- 2026-06-17: **Gallery receipt save fix:** Gallery picks always copy to **Pictures/Mountain Money** via `ReceiptPickerUriNormalizer` (parity with camera); toast if picker returns after dialog closed; hint when OCR date is outside Start/End filter. Tests: `ReceiptPickerUriNormalizerTest`, `ReceiptDateFilterHelperTest`. Local `./gradlew` still requires JDK **8–15**.
- 2026-06-17: **Bills-period anchor fix:** Unified `BillsDayAnchor` — current-month start when a bills day has passed (May 15 + 10th → May 10); prior-month walk when none yet; multi-day period-end uses previous bills day in set (Apr 15 + [1,15] → Apr 1). Removed single-day always-previous-month bug. `BillsDayAnchorTest` updated.




- 2026-09-10: Automatic receipt-folder recovery for yesterday and same-day pictures, including metadata from failed streams and accessible unindexed files. Persist verified references across history/restart; preserve filename metadata through transfers/splits; remove manual recovery selection. Photo-library permission is requested on launch/resume when stored receipts exist so older album files are not hidden behind an owned-only MediaStore subset. Shared recovery coverage: 96.76% lines / 83.87% branches. APK/test APK builds pass; full unit suite has three reproduced baseline failures; lint has the same 43 baseline errors. See [verification details](docs/receipt-recovery-verification.md).
